import { writable } from "svelte/store";

export let totalCategoriasN1 = writable([]);

export let usuariosMoodle = writable([]);

export let usuarioSelecionado = writable([]);

export let cursosUsuarioSelecionado = writable([]);

export let disciplinasFiltradaCategoriaSelecionada = writable([]);

export let entradaFiltrada = writable([]);

export let infoCC = writable([]);

export let AIEAnalise = writable([]);

export let feedbacksTotal = writable([]);

export let bancoAI = writable([]);

export let feedbackAI = writable([]);

export let QZsCategoriaSelecionada = writable([]);